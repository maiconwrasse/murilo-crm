import React from "react";
import { createRoot } from "react-dom/client";
import MuriloCRM from "./MuriloCRM.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MuriloCRM />
  </React.StrictMode>
);
